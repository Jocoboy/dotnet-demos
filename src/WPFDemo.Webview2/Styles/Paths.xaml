<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <ResourceDictionary.MergedDictionaries>
        <ResourceDictionary Source="Icons.xaml" />
    </ResourceDictionary.MergedDictionaries>

    <Style TargetType="Path" x:Key="maxPath">
        <Setter Property="Width" Value="13.3" />
        <Setter Property="Height" Value="12" />
        <Setter Property="Data" Value="{StaticResource window}" />
        <Setter Property="Fill" Value="{Binding Foreground,RelativeSource={RelativeSource FindAncestor, AncestorType={x:Type Button}}}" />
        <Setter Property="Stretch" Value="Fill" />
        <Style.Triggers>
            <DataTrigger Binding="{Binding WindowState,ElementName=Window}" Value="Maximized">
                <Setter Property="Data" Value="{StaticResource standard_window}" />
            </DataTrigger>
        </Style.Triggers>
    </Style>
</ResourceDictionary>